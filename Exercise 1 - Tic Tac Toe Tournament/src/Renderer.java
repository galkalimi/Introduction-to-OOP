/**
 * Interface that represents a renderer for rendering a game board.
 */
public interface Renderer {
    /**
     * Renders a specified board.
     * @param board The board to render.
     */
    public void renderBoard(Board board);
}
